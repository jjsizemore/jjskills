---
name: querying-github-repository-mcp
description: 'Use when querying GitHub repositories, files, commits, branches, PRs, issues, review comments, or workflow data through MCP tools.'
metadata:
  user-invocable: 'true'
  argument-hint: Specify the GitHub resource you need (file, commit, PR, etc.).
---

# Querying Github Repository Mcp

Use this skill when querying GitHub repositories, files, commits, branches, PRs, issues, review comments, or workflow data through MCP tools.

## Guidance

- Confirm the trigger matches the current task before applying this workflow.
- Keep the work scoped to the named capability and prefer narrower repo-local overrides when present.
- Preserve relevant evidence, commands, paths, and validation results in the final handoff.
- Update this skill with more specific guidance when a repeatable failure mode or workflow detail emerges.
