---
name: automating-browser-playwright-cli
description: 'Use when automating browser interactions or debugging web pages with Playwright CLI workflows, including screenshots, DOM inspection, or high-throughput page actions.'
metadata:
  user-invocable: 'true'
  argument-hint: Describe the browser automation steps in plain text.
---

# Automating Browser Playwright Cli

Use this skill when automating browser interactions or debugging web pages with Playwright CLI workflows, including screenshots, DOM inspection, or high-throughput page actions.

## Guidance

- Confirm the trigger matches the current task before applying this workflow.
- Keep the work scoped to the named capability and prefer narrower repo-local overrides when present.
- Preserve relevant evidence, commands, paths, and validation results in the final handoff.
- Update this skill with more specific guidance when a repeatable failure mode or workflow detail emerges.
