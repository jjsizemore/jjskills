# Pressure scenarios — resuming-work

## P2 Fresh session
Second process reads handoff only; does not redo passes:true.

## P5 Base drift
Detects branch/base change; reconciles before implement.

## P6 After cancel
ACTIVE missing → clear error unless explicit --run-id to inspect history.

## P9 Outer loop
Two process spawns; second is true cold start.
