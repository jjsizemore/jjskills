# Pressure Scenarios

## Scenario

A job skips on pull requests but runs on pushes.

## Required evidence

Real event fixtures, event-aware refs, reusable workflow inputs, secrets/fork
boundaries, local limitations, contract tests, and hosted verification.

## Pass criteria

Local emulation supports diagnosis; fresh hosted runs on the fixed commit remain
final proof.
