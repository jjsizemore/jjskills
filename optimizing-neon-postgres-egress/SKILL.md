---
name: optimizing-neon-postgres-egress
description: 'Use when diagnosing or reducing excessive Postgres egress, high database bills, unexpected data transfer, or query patterns that pull too much data.'
---

# Optimizing Neon Postgres Egress

Use this skill when diagnosing or reducing excessive Postgres egress, high database bills, unexpected data transfer, or query patterns that pull too much data.

## Guidance

- Confirm the trigger matches the current task before applying this workflow.
- Keep the work scoped to the named capability and prefer narrower repo-local overrides when present.
- Preserve relevant evidence, commands, paths, and validation results in the final handoff.
- Update this skill with more specific guidance when a repeatable failure mode or workflow detail emerges.
